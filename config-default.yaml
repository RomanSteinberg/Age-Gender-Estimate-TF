general:
  working_dir: ./
  logging_level: INFO


prepare:
  general:
    dataset_path: data/dataset.json
    test_size: 0.2
    processed_dataset_path: data/processed_data
    n_jobs: 8
  image:
    size: 256
    face_score_threshold: 0.75
    face_area_threshold: 2500

train:
  init:
    dataset_path: data/imdb_1k/dataset.json  # путь к json-файлу с описанием датасета
    model:   # имя модели для загрузки с диска
    save_frequency: 5  # частота сохранения моделей
  learning_rate: 1e-3
  weight_decay: 1e-5
  epoch: 1
  batch_size: 16  # 30 максимум для GeForce 1060
  keep_prob: 0.8
  cuda: True





